// Перша літера рядка (get-first-character)
// Напиши функцію getFirstChar(str), яка повертає перший символ рядка.

function getFirstChar(str) {
  return str[0];
}

console.log(getFirstChar("Hello")); // "H"
console.log(getFirstChar("JavaScript")); // "J"
