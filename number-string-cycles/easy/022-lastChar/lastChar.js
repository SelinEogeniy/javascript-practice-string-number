// 2. Останній символ

// Напиши функцію lastChar(str), яка повертає останній символ переданого рядка.

function lastChar(str) {
  return str[str.length - 1];
}

console.log(lastChar("Hello")); // "o"
console.log(lastChar("JavaScript")); // "t"
