// Перевірка на парність (is-even)
// Напиши функцію isEven(num), яка повертає true, якщо число парне, і false — якщо непарне.

function isEven(num) {
  return num % 2 === 0;
}

console.log(isEven(4)); // true
console.log(isEven(7)); // false
