// Check Adult (is-adult)
// Напиши функцію isAdult(age), яка повертає true, якщо age >= 18, інакше false.

function isAdult(age) {
  return age >= 18;
}

console.log(isAdult(20)); // true
console.log(isAdult(16)); // false
